void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;

	
}

void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;
	t_s_so *const	syl = stf->syls + pos;

}

void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;
	t_s_so *const	syl = stf->syls + pos;

}

void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;
	t_s_so *const	syl = stf->syls + pos;

}

void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;
	t_s_so *const	syl = stf->syls + pos;

}

void							ck_hour(
	int pos,
	void *p)
{
	t_s_kcs *const	stf = (t_s_kcs*)p;
	t_s_so *const	syl = stf->syls + pos;

}
